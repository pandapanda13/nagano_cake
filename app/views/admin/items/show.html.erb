<div class="container">
 <div class="row my-2">
  <p class="title col-2 offset-1 px-4">商品詳細</p>
 </div>
 <div class="row mt-5">
  <div class="col-5">
    <%= image_tag @item.image_id, size: "300x240" %>
  </div>

  <div class="col-2">
    <p class="">商品名</p>
    <p class="">商品説明</p>
    <p class="">ジャンル</p>
    <p class="">税込価格（税抜価格）</p>
    <p class="">販売ステータス</p>

   <%= link_to "編集する", edit_admin_item_path(@item.id), class: 'btn btn-success' %>
  </div>
  <div class="col-4">
    <p><%= @item.name %></p>
    <p><%= @item.introduction %></p>
    <p><%= @item.genre.name %></p>
    <p><%= @item.add_tax_price.to_s (:delimited) %>
    (
    <%= @item.price.to_s (:delimited) %>)円
    <% if @item.is_active == true %></p>
       <p class="text-success font-weight-bold"><%= is_active = "販売中" %></p>
      <% else %>
       <p class="text-danger font-weight-bold"><%= is_active = "販売停止中" %></p>
      <% end %>
  </div>
 </div>
</div>











