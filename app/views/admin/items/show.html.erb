<h2>商品詳細</h2>

<%= image_tag @item.image_id, size: "300x300" %>

<h5>商品名</h5>
<%= @item.name %>

<h5>商品説明</h5>
<%= @item.introduction %>

<h5>ジャンル</h5>
<%= @item.genre.name %>

<h5>税込価格（税抜価格）</h5>
<%= @item.add_tax_price.to_s (:delimited) %>
(
<%= @item.price.to_s (:delimited) %>)円


<h5>販売ステータス</h5>
<% if @item.is_active == true %>
  <%= is_active = "販売中" %>
<% else %>
  <%= is_active = "販売停止中" %>
<% end %><br>

<%= link_to "編集する", edit_admin_item_path(@item.id) %>



